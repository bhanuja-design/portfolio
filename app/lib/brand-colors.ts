import brandColorsJson from "@/app/lib/brand-colors.json";

/**
 * Brand color primitives for non-CSS consumers (metadata, favicons).
 * Hex values must stay in sync with @theme in app/styles/tokens.css.
 */
export const brandColors = brandColorsJson;
